# Copy to .env and fill real values. Do NOT commit .env with secrets.
# Example variables
PORT=5000
DB_URL=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>
ACCESS_SECRET=your_access_token_secret_here
REFRESH_SECRET=your_refresh_token_secret_here
GEMINI_API_KEY=
# OneCompiler API (hosted external service only; no Docker required)
ONECOMPILER_API_URL=https://onecompiler.com
ONECOMPILER_API_KEY=your-onecompiler-key
ONECOMPILER_AUTH_HEADER=Authorization
ONECOMPILER_AUTH_TYPE=bearer
ONECOMPILER_EXTRA_HEADERS={"Content-Type":"application/json"}

# Legacy / compatibility values kept only for older implementations
JUDGE0_API_URL=
JUDGE0_API_KEY=
JUDGE0_AUTH_HEADER=X-Auth-Token
JUDGE0_AUTH_TYPE=token
JUDGE0_URL=

# FRONTEND_URL / CLIENT_URL used for invite link generation
FRONTEND_URL=https://your-vercel-url.vercel.app
CLIENT_URL=https://your-vercel-url.vercel.app
